# R299 — Real-Candidate Timer Observation Certificate

Repo path:
  /home/josue/workspace/kernel/ai-agent-orchestrator-main

Branch:
  main

Phase classification:
  Primary: EXTENSION OF EXISTING CAPABILITY
  Secondary: TIMER OBSERVABILITY / REAL-CANDIDATE BRIDGE CERTIFICATION / LIVE SAFETY AUDIT
  Duplicate risk level: HIGH

Why duplicate risk exists:
  Existing capabilities already include:
    - R286 fresh live-qualified trigger watch
    - R287 autonomous trigger loop
    - R288 autonomous trigger scheduler
    - R292/R293 timer health
    - R294 dry-run lane arming rehearsal
    - R295 timer-observed armed-lane wait certificate
    - R296/R297 test-only matching trigger certificates
    - R298 real-candidate dry-run trigger bridge
    - final console panels
  R299 must reuse these. Do not create a parallel scheduler, parallel timer, parallel candidate watcher, parallel bridge, or order path.

Current state:
  - R298 committed and clean.
  - R298 reads the real fresh-trigger watcher.
  - Current expected real candidate state is:
      FRESH_TRIGGER_WAIT
      current_real_candidate_exists=false
      status=REAL_CANDIDATE_DRY_RUN_TRIGGER_BRIDGE_READY_TO_WAIT
  - R298 uses no fake candidate:
      test_only=false
      fake_candidate_used=false
  - No live order path is available.
  - No final submit command is available.

Purpose:
  Certify that the running dry-run scheduler/timer can observe the R298 real-candidate bridge state over recent ticks and that the system remains safe while waiting for a real matching fresh candidate.

Hard goal:
  Add a certificate that proves:
    - timer is active
    - recent scheduler ticks exist
    - R298 real-candidate bridge is available
    - real-candidate bridge state is READY_TO_WAIT when no real candidate exists
    - no fake/test candidate is used
    - no submit/order/executable payload is possible
    - final console exposes this state clearly

Operator doctrine:
  - Operator arms/disarms/tunes risk/kills system.
  - Machine auto-triggers only when armed and all gates are open.
  - Operator does not approve each signal.
  - R299 remains dry-run observation/certification only.
  - Alerts are visibility/audit, not permission.

Non-negotiable safety:
  - Do NOT place orders.
  - Do NOT call Binance /fapi/v1/order.
  - Do NOT call Binance test order endpoint.
  - Do NOT call Binance leverage/margin mutation endpoints.
  - Do NOT enable live execution.
  - Do NOT set HAMMER_LIVE_EXECUTION_ENABLED=true.
  - Do NOT set HAMMER_ALLOW_LIVE_ORDERS=true.
  - Do NOT disable global kill switch.
  - Do NOT mutate env files.
  - Do NOT mutate live config.
  - Do NOT mutate risk contracts.
  - Do NOT create executable order payloads.
  - Do NOT make final live submit command available.
  - Do NOT print secrets, signatures, signed URLs, API keys, or env values.
  - Do NOT require per-signal operator approval.
  - Do NOT install/start/stop/restart/enable/disable systemd.
  - Do NOT run sudo.
  - Do NOT write to /etc/systemd/system.
  - Do NOT use test-only fake candidate input.
  - Do NOT modify installed timer templates.
  - R299 must only observe real-candidate bridge output and timer/scheduler status.

Allowed:
  - Modify repo code/docs/tests.
  - Read existing configs/logs.
  - Read real fresh-candidate watcher output.
  - Read R298 bridge output.
  - Read timer health using existing read-only helper.
  - Read scheduler ledger/status.
  - Write repo-local R299 ledger only when explicit record flag is used.
  - Add API/CLI/final-console visibility.
  - Add print-only script.
  - Add tests using fixtures/mocks.

Required capability scan:
  Inspect and reuse before coding:
    - src/app/hammer_radar/operator/tiny_live_real_candidate_dry_run_trigger_bridge.py
    - src/app/hammer_radar/operator/tiny_live_fresh_trigger_watch.py
    - src/app/hammer_radar/operator/tiny_live_autonomous_trigger_scheduler.py
    - src/app/hammer_radar/operator/tiny_live_autonomous_trigger_scheduler_timer_health.py
    - src/app/hammer_radar/operator/tiny_live_autonomous_trigger_loop.py
    - src/app/hammer_radar/operator/tiny_live_timer_observed_armed_lane_wait_certificate.py
    - src/app/hammer_radar/operator/tiny_live_dry_run_lane_arming_rehearsal.py
    - src/app/hammer_radar/operator/tiny_live_final_console.py
    - src/app/hammer_radar/operator/inspect.py
    - src/app/hammer_radar/operator/approval_api.py
    - logs/hammer_radar_forward/tiny_live_autonomous_trigger_scheduler.ndjson
    - logs/hammer_radar_forward/tiny_live_real_candidate_dry_run_trigger_bridge.ndjson
    - logs/hammer_radar_forward/tiny_live_fresh_trigger_watch.ndjson
    - configs/hammer_radar/autonomous_arming_state.json
    - configs/hammer_radar/tiny_live_risk_contracts.json
    - docs/hammer_radar/live_readiness/PHASE_INDEX.md
    - tests/hammer_radar/

Implementation requirements:

1. Add module:
   src/app/hammer_radar/operator/tiny_live_real_candidate_timer_observation_certificate.py

   Packet:
     event_type="TINY_LIVE_REAL_CANDIDATE_TIMER_OBSERVATION_CERTIFICATE"
     created_by_phase="R299_REAL_CANDIDATE_TIMER_OBSERVATION_CERTIFICATE"

   Required fields:
     - status:
         REAL_CANDIDATE_TIMER_OBSERVATION_READY_TO_WAIT_CERTIFIED
         REAL_CANDIDATE_TIMER_OBSERVATION_TRIGGER_CERTIFIED
         REAL_CANDIDATE_TIMER_OBSERVATION_BLOCKED
     - requested_lane_key
     - allowed_lane_keys
     - lane_is_live_qualified
     - exact_lane_only=true
     - no_cross_lane_borrowing=true
     - real_candidate_source="fresh_trigger_watch_via_r298_bridge"
     - test_only=false
     - fake_candidate_used=false
     - dry_run_only=true
     - live_execution_enabled=false
     - allow_live_orders=false
     - global_kill_switch=true
     - timer_health_status
     - timer_active
     - timer_loaded
     - recent_tick_seen
     - recent_tick_count
     - scheduler_recent_ticks_observed
     - scheduler_latest_status
     - scheduler_latest_trigger_loop_status
     - scheduler_latest_candidate_lane_key
     - r298_bridge_status
     - r298_bridge_record_seen
     - current_real_candidate_exists
     - current_real_candidate_lane_key
     - current_real_candidate_signal_id
     - current_real_candidate_freshness_status
     - current_real_candidate_live_qualification_class
     - candidate_matches_requested_lane
     - simulated_dry_run_trigger_recorded
     - simulated_lifecycle_status
     - simulated_open_record
     - simulated_protective_orders
     - simulated_close_plan
     - no_matching_candidate_action="WAIT"
     - observation_summary
     - final_command_available=false
     - submit_allowed=false
     - real_order_forbidden=true
     - executable_payload_created=false
     - order_payload_created=false
     - order_placed=false
     - binance_order_endpoint_called=false
     - binance_test_order_endpoint_called=false
     - per_signal_operator_approval_required=false
     - blockers
     - safety
     - real_candidate_timer_observation_certificate_panel

2. Expected no-candidate behavior:
   If R298 bridge says READY_TO_WAIT:
     status=REAL_CANDIDATE_TIMER_OBSERVATION_READY_TO_WAIT_CERTIFIED
     current_real_candidate_exists=false
     simulated_dry_run_trigger_recorded=false
     no_matching_candidate_action="WAIT"
     blockers=[]

3. Future real-candidate behavior:
   If R298 bridge says CERTIFIED:
     status=REAL_CANDIDATE_TIMER_OBSERVATION_TRIGGER_CERTIFIED
     current_real_candidate_exists=true
     candidate_matches_requested_lane=true
     simulated_dry_run_trigger_recorded=true
     simulated lifecycle copied from R298
     still no final command / submit / order

4. Blocked behavior:
   If R298 bridge says BLOCKED:
     status=REAL_CANDIDATE_TIMER_OBSERVATION_BLOCKED
     blockers should preserve R298 blockers
     no submit/order/executable payload

   If no recent timer tick:
     status=REAL_CANDIDATE_TIMER_OBSERVATION_BLOCKED
     blockers include timer_recent_tick_missing

   If no scheduler recent ticks:
     blockers include scheduler_recent_tick_missing

5. Real candidate only:
   R299 must not inject candidates.
   R299 must not accept simulation flags.
   R299 must not call R296/R297 test-only simulation.
   R299 must assert:
     test_only=false
     fake_candidate_used=false

6. CLI:
   Add inspect command:
     tiny-live-real-candidate-timer-observation-certificate

   Args:
     --lane-key <lane>
     --operator-id <id>
     --reason <text>
     --record-real-candidate-timer-observation-certificate

   No simulate args are allowed for this command.

7. API:
   Add:
     GET /tiny-live/real-candidate-timer-observation-certificate/status

   Safe default:
     - lane defaults to BTCUSDT|44m|long|ladder_close_50_618
     - no record
     - no simulation
     - no order
     - no submit
     - final_command_available=false
     - submit_allowed=false
     - real_order_forbidden=true

   Optional query param:
     ?lane_key=...

   API must never record R299 ledger.

8. Final console:
   Add:
     real_candidate_timer_observation_certificate_panel

   It should show:
     - status
     - requested lane
     - timer/scheduler observation summary
     - R298 bridge summary
     - real candidate summary
     - simulated lifecycle summary if present
     - blockers
     - recommended_next_operator_move
     - final_command_available=false
     - submit_allowed=false
     - real_order_forbidden=true

9. Print-only script:
   Add:
     scripts/hammer_print_r299_real_candidate_timer_observation_certificate_plan.sh

   Must print commands only.
   It must not run sudo/systemctl/curl/order/install/cp/mv/rm.
   It may print curl commands as text, not execute them.

   Include:
     - timer health check
     - scheduler status check
     - R298 bridge status check
     - R299 CLI certificate command
     - R299 API status command
     - final console check
     - safety grep
     - cleanup guidance

   Banner:
     R299 PRINT ONLY - real-candidate timer observation certificate plan

10. Docs:
   Add:
     docs/hammer_radar/live_readiness/R299_REAL_CANDIDATE_TIMER_OBSERVATION_CERTIFICATE.md

   Update:
     docs/hammer_radar/live_readiness/PHASE_INDEX.md

   Explain:
     - R299 observes R298 real-candidate bridge through timer/scheduler context
     - READY_TO_WAIT is expected if no real candidate exists
     - CERTIFIED only if R298 certifies a real matching candidate
     - no fake/test candidates are used
     - no live order can occur
     - expected next phase

11. Tests:
   Add/extend tests for:
     - R298 READY_TO_WAIT + timer active + scheduler ticks => READY_TO_WAIT_CERTIFIED
     - R298 CERTIFIED fixture => TRIGGER_CERTIFIED
     - R298 BLOCKED fixture => BLOCKED preserving blockers
     - timer missing blocks
     - scheduler recent tick missing blocks
     - invalid lane blocks
     - exact_lane_only=true
     - no_cross_lane_borrowing=true
     - test_only=false
     - fake_candidate_used=false
     - no simulate args accepted
     - API plain status never records
     - final console includes panel
     - final_command_available=false always
     - submit_allowed=false always
     - real_order_forbidden=true always
     - executable_payload_created=false always
     - order_payload_created=false always
     - order_placed=false always
     - binance_order_endpoint_called=false always
     - binance_test_order_endpoint_called=false always

Validation:
  PYTHONPATH=. .venv/bin/python -m pytest -q tests/hammer_radar \
    -k "real_candidate_timer_observation_certificate or real_candidate_dry_run_trigger_bridge or autonomous_trigger_scheduler or timer_health or final_console or approval_api"

  sudo systemctl restart hammer-approval-api.service
  sleep 2
  systemctl is-active hammer-approval-api.service
  ss -ltnp | grep ':8015' || true

  echo "=== R299 PRINT ONLY PLAN ==="
  bash scripts/hammer_print_r299_real_candidate_timer_observation_certificate_plan.sh | sed -n '1,380p'

  echo "=== TIMER HEALTH ==="
  curl -sS http://127.0.0.1:8015/tiny-live/autonomous-trigger-scheduler/timer-health | jq '{
    event_type,
    status,
    timer_active,
    timer_loaded,
    recent_tick_seen,
    recent_tick_count,
    documentation_warning_seen,
    installed_unit_refresh_required,
    final_command_available,
    submit_allowed,
    real_order_forbidden,
    safety
  }'

  echo "=== SCHEDULER STATUS ==="
  curl -sS http://127.0.0.1:8015/tiny-live/autonomous-trigger-scheduler/status | jq '{
    event_type,
    status,
    current_fresh_candidate_exists,
    current_candidate_lane_key,
    exact_lane_auto_armed,
    latest_scheduler_status,
    latest_trigger_loop_status,
    final_command_available,
    submit_allowed,
    real_order_forbidden,
    safety
  }'

  echo "=== R298 BRIDGE STATUS ==="
  curl -sS http://127.0.0.1:8015/tiny-live/real-candidate-dry-run-trigger-bridge/status | jq '{
    event_type,
    status,
    current_real_candidate_exists,
    current_real_candidate_lane_key,
    candidate_matches_requested_lane,
    test_only,
    fake_candidate_used,
    simulated_dry_run_trigger_recorded,
    final_command_available,
    submit_allowed,
    real_order_forbidden,
    safety
  }'

  echo "=== R299 CLI CERTIFICATE ==="
  PYTHONPATH=. .venv/bin/python -m src.app.hammer_radar.operator.inspect \
    --log-dir logs/hammer_radar_forward \
    tiny-live-real-candidate-timer-observation-certificate \
    --lane-key "BTCUSDT|44m|long|ladder_close_50_618" \
    --operator-id local_operator \
    --reason "R299 real-candidate timer observation certificate; no fake candidate; no submit; no order." \
    --record-real-candidate-timer-observation-certificate \
    | tee /tmp/r299_real_candidate_timer_observation_certificate.json \
    | jq '{
      event_type,
      status,
      requested_lane_key,
      lane_is_live_qualified,
      exact_lane_only,
      no_cross_lane_borrowing,
      real_candidate_source,
      test_only,
      fake_candidate_used,
      dry_run_only,
      live_execution_enabled,
      allow_live_orders,
      global_kill_switch,
      timer_health_status,
      timer_active,
      timer_loaded,
      recent_tick_seen,
      recent_tick_count,
      scheduler_recent_ticks_observed,
      scheduler_latest_status,
      scheduler_latest_trigger_loop_status,
      scheduler_latest_candidate_lane_key,
      r298_bridge_status,
      r298_bridge_record_seen,
      current_real_candidate_exists,
      current_real_candidate_lane_key,
      current_real_candidate_signal_id,
      current_real_candidate_freshness_status,
      current_real_candidate_live_qualification_class,
      candidate_matches_requested_lane,
      simulated_dry_run_trigger_recorded,
      simulated_lifecycle_status,
      simulated_open_record,
      simulated_protective_orders,
      simulated_close_plan,
      no_matching_candidate_action,
      observation_summary,
      final_command_available,
      submit_allowed,
      real_order_forbidden,
      executable_payload_created,
      order_payload_created,
      order_placed,
      binance_order_endpoint_called,
      binance_test_order_endpoint_called,
      per_signal_operator_approval_required,
      blockers,
      safety
    }'

  echo "=== R299 API STATUS ==="
  curl -sS http://127.0.0.1:8015/tiny-live/real-candidate-timer-observation-certificate/status | jq '{
    event_type,
    status,
    real_candidate_timer_observation_certificate_panel,
    final_command_available,
    submit_allowed,
    real_order_forbidden,
    safety
  }'

  echo "=== FINAL CONSOLE ==="
  curl -sS http://127.0.0.1:8015/tiny-live/final-console | jq '{
    status,
    real_candidate_timer_observation_certificate_panel,
    real_candidate_dry_run_trigger_bridge_panel,
    timer_integrated_test_only_matching_trigger_rehearsal_panel,
    timer_observed_armed_lane_wait_certificate_panel,
    dry_run_lane_arming_rehearsal_panel,
    final_live_submit_command_packet,
    recommended_next_operator_move,
    safety
  }'

  echo "=== SAFETY GREP ==="
  grep -R "\"order_placed\": true\|\"binance_order_endpoint_called\": true\|\"binance_test_order_endpoint_called\": true\|\"submit_attempted\": true\|\"real_order_placed\": true\|\"final_command_available\": true\|\"submit_allowed\": true\|\"executable_payload_created\": true\|\"secrets_shown\": true\|\"live_execution_enabled\": true\|\"allow_live_orders\": true\|\"fake_candidate_used\": true" -n \
    logs/hammer_radar_forward/tiny_live*.ndjson \
    logs/hammer_radar_forward/*autonomous*.ndjson \
    logs/hammer_radar_forward/*binance*.ndjson \
    2>/dev/null || true

Report:
  - files changed
  - tests run
  - timer health status
  - scheduler observed status
  - R298 bridge status
  - R299 certificate status
  - whether real candidate exists
  - proof no fake/test candidate was used
  - API/final console panel summary
  - proof no order/submit/executable payload/live enable/secrets
  - runtime log dirt generated and cleanup recommendation
  - recommended next phase
