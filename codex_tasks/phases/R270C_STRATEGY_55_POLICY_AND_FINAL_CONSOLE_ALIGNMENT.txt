# R270C — Strategy 55% Policy and Final Console Alignment

Repo path:
  /home/josue/workspace/kernel/ai-agent-orchestrator-main

Branch:
  main

Phase classification:
  Primary: POLICY HARDENING / FINAL READINESS
  Secondary: STRATEGY QUALIFICATION, UI CONSISTENCY, SAFETY HARDENING
  Duplicate risk level: LOW-MEDIUM

Why this phase exists:
  Operator policy changed:
    Any BTCUSDT lane/timeframe/direction can be considered for tiny live only if strategy evidence is strong enough:
      - win_rate_pct >= 55.0
      - sample_count >= minimum sample count, default/configured 30
      - exact lane risk contract exists or is generated by guarded policy
      - signal origin is allowed
      - betrayal/inverse is blocked unless explicitly accepted
      - max notional remains 80 USDT
      - leverage remains 10x

  R270B added strategy-qualified lane selection, but verification still shows:
    - strategy-promotion/status config min_win_rate=45.0
    - BTCUSDT|13m|long with 47.27% is still marked promotion_ready
    - old allowed/paper_only/blocked timeframe buckets still appear as policy
    - final console can show stale 0.006 BTC / ~393 USDT risk context while JIT sees fresh 0.001 BTC / ~66 USDT context

Hard goal:
  Make 55% win rate the actual tiny-live qualification policy across promotion, readiness, trade-ticket, final console, and JIT.
  Let any BTCUSDT lane/timeframe qualify by evidence, not by old fixed timeframe buckets.
  Align final console with the same current ticket/JIT context and avoid stale risk panels.

Non-negotiable safety:
  - Do NOT place real orders.
  - Do NOT call Binance /fapi/v1/order.
  - Do NOT call Binance test order endpoint.
  - Do NOT enable live execution.
  - Do NOT disable kill switches.
  - Do NOT execute final submit.
  - Do NOT make final live command available in diagnostic mode.
  - Do NOT allow 45% win-rate strategies into tiny-live eligibility.
  - Do NOT allow timeframe allowlists to override weak evidence.
  - Do NOT allow cross-lane risk contract borrowing.
  - If uncertain, remain BLOCKED.

Required scan:
  Inspect:
    - src/app/hammer_radar/operator/strategy_promotion_watcher.py
    - src/app/hammer_radar/operator/readiness.py
    - src/app/hammer_radar/operator/trade_ticket.py
    - src/app/hammer_radar/operator/tiny_live_strategy_lane_selection.py
    - src/app/hammer_radar/operator/tiny_live_final_console.py
    - src/app/hammer_radar/operator/tiny_live_jit_launch_packet.py
    - src/app/hammer_radar/operator/tiny_live_risk_contract_validation.py
    - configs/hammer_radar/tiny_live_risk_contracts.json
    - configs/hammer_radar/lane_controls.json
    - tests/hammer_radar touching strategy promotion, readiness, trade ticket, final console, JIT, strategy lane selection

Implementation:
  1. Global 55% tiny-live evidence policy:
     - Use min_win_rate_pct=55.0 for tiny-live lane qualification.
     - Keep sample_count >= configured min_sample or default 30.
     - A lane below 55 must not be promotion_ready / tiny-live eligible.
     - BTCUSDT|13m|long at 47.27% must not appear as tiny-live qualified.
     - BTCUSDT|44m|long at 58.57% may qualify if sample_count >= 30 and other gates pass.

  2. All TFs allowed through evidence:
     - Do not hard-block a timeframe solely because it is in old paper_only_timeframes if its evidence qualifies.
     - Old timeframe buckets may remain as historical/reporting context, but they must not override the 55% evidence policy.
     - Surface this clearly:
       evidence_policy_all_timeframes_enabled: true
       min_win_rate_pct: 55.0
       min_sample_count: 30

  3. Strategy promotion endpoint:
     - /strategy-promotion/status must show tiny-live policy threshold 55.
     - promotion_ready should include only lanes meeting >=55 and sample minimum.
     - Lanes between 45 and 55 should be reported as below_operator_55_policy or similar, not eligible.

  4. Readiness / trade-ticket:
     - Readiness should only become READY if the fresh candidate’s lane passes the 55% strategy qualification.
     - Trade-ticket should not be PROPOSED for a fresh candidate whose strategy evidence is below 55 or unknown.
     - If blocked, expose:
       strategy_win_rate_below_55
       strategy_sample_count_below_minimum
       strategy_evidence_missing
       exact_lane_risk_contract_missing
       as applicable.

  5. Final console / JIT alignment:
     - Final console should use current proposed ticket context when one exists.
     - If no current proposed ticket exists, do not display stale 0.006 / 393 USDT as active candidate risk.
     - Show stale context only under a clearly labeled previous_r264_preview or historical panel.
     - Active risk_contract_interpretation should reflect either:
       - current proposed ticket, or
       - no_current_proposed_ticket
     - JIT and final console must agree on current lane / no lane.

  6. Betrayal visibility:
     - Preserve signal_origin_family, betrayal_mode_involved, betrayal_inverse_involved.
     - Betrayal/inverse remains blocked unless future explicit acceptance exists.

  7. Tests:
     Add/update tests proving:
       - min win rate for tiny-live policy is 55.
       - 47.27% does not qualify.
       - 58.57% qualifies if sample count >= 30 and origin/risk gates pass.
       - all TFs can qualify by evidence, not fixed allowlist.
       - paper_only timeframe can qualify if evidence >=55 and sample >=30.
       - readiness blocks fresh candidate below 55.
       - trade-ticket blocks below-55 or missing evidence.
       - final console does not show stale 393 USDT as active candidate when no current ticket exists.
       - no order placed, no submit, no Binance order endpoint.

Validation:
  PYTHONPATH=. .venv/bin/python -m pytest -q tests/hammer_radar -k "strategy_promotion or tiny_live_strategy_lane_selection or readiness or trade_ticket or tiny_live or final_console or jit_launch"

  sudo systemctl restart hammer-approval-api.service
  systemctl is-active hammer-approval-api.service

  curl -s http://127.0.0.1:8015/strategy-promotion/status | jq .
  curl -s http://127.0.0.1:8015/readiness | jq .
  curl -s http://127.0.0.1:8015/trade-ticket | jq .
  curl -s http://127.0.0.1:8015/tiny-live/final-console | jq .

  PYTHONPATH=. .venv/bin/python -m src.app.hammer_radar.operator.inspect \
    --log-dir logs/hammer_radar_forward \
    tiny-live-jit-launch-packet \
    --run-jit-launch-prep \
    --record-jit-launch-packet \
    --confirm-jit-launch-prep "I CONFIRM TINY LIVE JIT LAUNCH PREP ONLY; REFRESH CONTRACT-FIT TRIPLET, ARM R263 EXPERIMENTAL LANE, RUN R264 DRY PREVIEW; NO SUBMIT; NO ORDER; NO BINANCE ORDER CALL." \
    --operator-id local_operator \
    --reason "R270C 55 percent all-timeframe evidence policy diagnostic; no submit."

Report:
  - files changed
  - strategy policy threshold now used
  - which lanes qualify under >=55
  - whether 13m long 47.27 is blocked
  - whether 44m long 58.57 qualifies
  - current readiness/trade-ticket status
  - final console active candidate context
  - JIT status
  - safety confirmation: no order, no submit, no Binance order endpoint
